# 開発環境 - モックサーバー
# フォールバック機能により、localhost → 172.20.10.4 の順に自動接続を試みます
# api.ts内で直接設定しているため、この環境変数は現在未使用です
# 将来的にURLを変更する場合は、api.tsのconstructor内のbaseUrlsを編集してください

# VITE_API_BASE_URL=http://localhost:3001/api
# VITE_USE_MOCK=true
